{
  "name": "chat-platform-netlify",
  "version": "1.0.0",
  "scripts": {
    "build": "cd Frontend && npm install && npm run build",
    "install-backend": "cd BackendNew && npm install"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "@google/generative-ai": "^0.24.1",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.0.1",
    "serverless-http": "^3.2.0"
  }
}